{
	"$schema": "https://www.schemastore.org/package.json",
	"name": "mono",
	"private": true,
	"type": "module",
	"module": "index.ts",
	"scripts": {
		"fm": "oxfmt --write",
		"lint": "oxlint --fix",
		"dev": "concurrently 'bun run --cwd=./packages/frontend dev' 'bun run --cwd=./packages/backend dev'",
		"api:gen": "bun --cwd=./packages/backend api:gen"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"oxfmt": "^0.46.0",
		"oxlint": "^1.61.0"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
