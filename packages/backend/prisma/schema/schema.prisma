generator client {
    output                 = "../generated"
    provider               = "prisma-client"
    engineType             = "client"
    runtime                = "bun"
    moduleFormat           = "esm"
    generatedFileExtension = "ts"
    importFileExtension    = "ts"
}

datasource db {
    provider = "sqlite"
}
